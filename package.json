{
  "name": "odontobackend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "NODE_ENV=production node ./src/server.js",
    "dev": "clear && NODE_ENV=development nodemon ./src/server.js",
    "test": "NODE_ENV=test jest",
    "migrate": "NODE_ENV=production yarn sequelize-cli db:migrate",
    "undo-migrate": "NODE_ENV=production yarn sequelize-cli db:migrate:undo:all",
    "migrate-dev": "NODE_ENV=development yarn sequelize-cli db:migrate",
    "migrate-dev-undo": "NODE_ENV=development yarn sequelize-cli db:migrate:undo:all",
    "pretest": "NODE_ENV=test yarn sequelize-cli db:migrate",
    "posttest": "NODE_ENV=test yarn sequelize-cli db:migrate:undo:all"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-rate-limit": "^5.2.6",
    "helmet": "^4.6.0",
    "jsonwebtoken": "^8.5.1",
    "moment": "^2.29.1",
    "nodemailer": "^6.6.0",
    "pg": "^8.6.0",
    "pg-hstore": "^2.3.3",
    "sequelize": "^6.6.2",
    "validator": "^13.6.0"
  },
  "devDependencies": {
    "jest": "^27.0.5",
    "nodemon": "^2.0.7",
    "sequelize-cli": "^6.2.0",
    "supertest": "^6.1.3"
  }
}

